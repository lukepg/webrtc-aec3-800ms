diff --git a/api/audio/echo_canceller3_config.h b/api/audio/echo_canceller3_config.h
index abc123..def456 100644
--- a/api/audio/echo_canceller3_config.h
+++ b/api/audio/echo_canceller3_config.h
@@ -78,13 +78,19 @@ struct RTC_EXPORT EchoCanceller3Config {
       float noise_gate;
     };

-    RefinedConfiguration refined = {13,     0.00005f, 0.05f,
+    // 800ms support: 40 blocks = 40 * 4ms = 160ms filter + margin = ~200ms max delay
+    // Reduced leakage rates for longer filter to prevent over-adaptation
+    RefinedConfiguration refined = {40,     0.00002f, 0.02f,
                                     0.001f, 2.f,      20075344.f};
-    CoarseConfiguration coarse = {13, 0.7f, 20075344.f};
+    CoarseConfiguration coarse = {40, 0.5f, 20075344.f};

-    RefinedConfiguration refined_initial = {12,     0.005f, 0.5f,
+    // Initial phase: more conservative adaptation for longer filter
+    RefinedConfiguration refined_initial = {40,     0.002f, 0.2f,
                                             0.001f, 2.f,    20075344.f};
-    CoarseConfiguration coarse_initial = {12, 0.9f, 20075344.f};
+    CoarseConfiguration coarse_initial = {40, 0.7f, 20075344.f};

+    // Increased duration for filter to build up sufficient history
     size_t config_change_duration_blocks = 250;
-    float initial_state_seconds = 2.5f;
+    // 800ms support: need longer initialization time (4s vs 2.5s)
+    float initial_state_seconds = 4.0f;
+    int coarse_reset_hangover_blocks = 40;  // Increased from 25 for longer filter
